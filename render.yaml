services:
  - type: web
    name: pixelmapper
    env: docker
    plan: free
    autoDeploy: true
    envVars:
      - key: PORT
        value: 8080
      - key: FRONTEND_DIST
        value: /app/frontend/dist
      - key: DATABASE_URL
        sync: false
